// musa = []
// musa.push(1) 
// .push() adds item at the last
// .pop removes item at the end
// push function helps you add something to the array

// var list = [1,2,3];

// list.push(4);

// console.log(list.push('3'))

var output = []; //empty array
var count = 0; // variable

function fizzBuzz(){ // function called fizzBuzz


    output.push[count]; // pushes the initial value of account into the empty array
    count++; // increments the count each time the code is run

    console.log(output); // prints the array

}

// console.log(fizzBuzz);
fizzBuzz(output);

// console.log(fizzBuzz(output.push[a]));