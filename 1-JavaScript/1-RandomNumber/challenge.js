// create a two prompts, that has the two names
// however, create a random number from 0-100
// the random number should display the love percentage of the two people


// prompts

sign = window.prompt('What is your name?');

// random number from 0 to 100
// step by step menthod
var number = Math.random();
number = number * 100; 
number = Math.floor() // rounds the number to decimal

// combines everything
console.log(Math.floor(Math.random() * 100)) 