$("button").on("click", function(){


    $("h1").slideUp().slideDown.animate({opacity: 0.5});
    // $("h1").animate({opacity: 0.5}); //only works with style that deals with numeric value

    // OR

    // $("h1".toggle);
    // .hide
    // .fadeout
    // /fadeToggle();
    // .slideUp()
    // .slideToggle
});

// web development is like open book exam, don't try to memorize things
// just understand it, and know where to find the documentation